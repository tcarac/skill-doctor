---
name: unclosed-frontmatter
description: Test skill with frontmatter that is not properly closed

# Test Skill - Unclosed Frontmatter

This fixture tests validation when the YAML frontmatter is missing the closing --- delimiter.
