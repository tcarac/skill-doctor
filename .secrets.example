# Example secrets file for Act
# Copy this file to .secrets and fill in values (don't commit .secrets!)
#
# Usage: cp .secrets.example .secrets
#

# GitHub token for API access (only needed for PR comments/checks features)
GITHUB_TOKEN=ghp_your_token_here

# Codecov token for coverage uploads (optional)
CODECOV_TOKEN=your_codecov_token_here
